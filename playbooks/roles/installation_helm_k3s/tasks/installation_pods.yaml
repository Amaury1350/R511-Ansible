- name: Installation de pod nginx avec des charts helm
  block:
    - name: Ajouter le repository stable
      command: helm repo add ingress-nginx https://kubernetes.github.io/ingress-nginx

    - name: Mettre à jour les repositories
      command: helm repo update

    - name: Installer le pod nginx
      command: helm install my-release ingress-nginx/ingress-nginx
  when: "'master' in inventory_hostname"